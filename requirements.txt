# Core stack. requests is the one addition versus a typical pandas/sklearn
# project - it's what talks to the World Bank API in src/data_loading.py.

pandas>=2.0,<4
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
requests>=2.28

# Optional:
#   tabulate     nicer markdown tables in the generated report
#   jupyter      to run the notebooks in notebooks/
